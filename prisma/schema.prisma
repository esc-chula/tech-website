// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model User {
    id              Int             @id @default(autoincrement())
    email           String          @unique
    password        String
    username        String          @unique
    urls            ShortenedLink[] @relation("UserToShortenedLink")
    qrCodes         QrCodeGen[]     @relation("UserToQrCodeGen")
}

model ShortenedLink {
    id              Int         @id @default(autoincrement())
    name            String      
    url             String
    slug            String      @unique
    editedAt        DateTime    @default(now())
    user            User        @relation(fields: [userId], references: [id], name:"UserToShortenedLink")
    userId          Int
}

model QrCodeGen {
    id              Int         @id @default(autoincrement())
    name            String
    url             String      @unique
    qrCode          String
    color           String
    logo            String?
    editedAt        DateTime    @default(now())
    user            User        @relation(fields: [userId], references: [id], name: "UserToQrCodeGen")
    userId          Int
}