'use client'
import CreateQRCode from '../_components/createQRCode';

export default function QRCodePage() {
    return (
        <div className="container">
            <CreateQRCode />
        </div>
    );
}